# 영어 읽기 도구 — 작업 인계 문서

이 파일과 `red-headed-league.html`, `word-game/` 폴더가 같은 위치에 있습니다.
코워크에서 이 폴더를 열고, 이 문서를 읽은 뒤 이어서 작업하면 됩니다.

## 만들려는 것

코난 도일의 단편 **The Red-Headed League** 원문을 읽으면서,
막히는 문장만 탭해서 구조를 펼쳐볼 수 있는 개인용 읽기 도구.
파일 하나로 돌아가는 HTML이고, 서버도 인터넷도 필요 없습니다.

## 왜 이렇게 만드는가 (설계 근거)

만드는 사람의 실제 문제는 **어휘가 아니라 구조**입니다.
쉬운 단어로 된 긴 문장에서 해석이 무너집니다.
영어는 핵심(주어+동사)을 먼저 던지고 뒤에 설명을 덧붙이는데,
한국어식으로 문장 끝에서부터 되짚어 해석하려 하면 긴 문장에서 반드시 막힙니다.

그래서 도구의 목표는 **앞에서부터 의미 단위로 끊어 읽는 감각**을 붙이는 것입니다.

지켜야 할 원칙:

- **모든 문장을 분해해서 보여주지 않는다.** 기본은 그냥 읽기.
  막힌 문장만 탭해서 펼친다. 전부 펼치면 읽기가 노동이 된다.
- **번역문은 맨 아래에 둔다.** 순서는 뼈대 → 원문 재독 → 덩어리 → 번역.
  번역부터 읽으면 아무것도 남지 않는다.
- **단어와 구조는 다른 문제다.** 단어를 알아도 안 읽히는 문장이 있고,
  그래서 단어 탭과 구조 펼치기가 따로 있다.
- **복습은 다음 날.** 같은 날 풀면 잔상으로 맞힌다.
  복습 문제는 이미 읽은 대목에서만 나온다.

## 현재 상태

`red-headed-league.html` 한 파일 안에 읽기 도구의 데이터와 화면이 모두 들어 있습니다. 탭 3개:

- 읽기: 12개 대목, 총 114문장 (소설 첫 문장부터 순서대로). 단어를 탭하면 발음기호(IPA)도 같이 보임.
- 뼈대: 읽기 탭에서 지금 보고 있는 대목(`st.p`)의 문장만, 영문 뼈대(`sk`)만 접어서 나열.
  탭하면 원문이 펼쳐지고 그 문장이 왜 안 읽히는지(`n`) 메모가 같이 보입니다. 한글 번역은
  일절 없음(뼈대의 한글 `skk`도, 원문 전체 번역 `ko`도) — 뼈대만 보고 원문을 머릿속으로
  재구성해보는 용도라 번역이 보이면 의미가 없어짐. 읽기 탭에서 대목을 넘기면 뼈대 탭도
  같이 따라 넘어갑니다. `sentences`의 sk/t/n을 그대로 재사용합니다 — 새 데이터 추가 없이
  자동으로 늘어남.
- 복습: 객관식 34문제, 읽은 대목(`st.seen`)에서만 출제. 문제는 읽은 대목 전체에서 무작위로 섞여
  나오므로(설계 의도), 복습 탭의 "N / 총"은 읽기 탭의 대목 번호("N / 12")와는 다른 걸 가리킵니다 —
  지금 읽기 탭에 떠 있는 대목과 복습 첫 문제의 문장이 다를 수 있는 게 정상입니다.
- 진도 저장: localStorage (브라우저별로 따로 저장됨)
- 상단 nav 오른쪽에 "단어 게임 ↗", "문장 완성 게임 ↗" 링크가 있어 각각 `word-game/index.html`,
  `sentence-game/index.html`을 새 탭으로 바로 엽니다.

뼈대 탭에서 문장을 펼치면, 원문 중 뼈대(`sk`)에 해당하는 부분이 파란 볼드로 강조됩니다
(원문과 뼈대의 토큰을 LCS로 정렬하는 `skeletonHits()` 함수 사용). 단, 뼈대가 원문의 절 순서를
뒤바꿔 재배열한 문장 하나는 두 구간 중 더 긴 쪽만 강조되는 알려진 한계가 있습니다(오작동 아님).

`word-game/` 폴더에는 단어 암기용 카드 매칭 게임이 별도로 들어 있습니다 (`index.html` 열면 실행).
`red-headed-league.html`의 `dict`에서 뽑은 단어 253개로 카드를 만들고, 6/8/12쌍(3종류 난이도)
중 골라 영어 카드와 뜻 카드를 뒤집어 짝을 맞춥니다. 영어 카드에는 발음기호도 작게 같이 나옵니다.
시도 횟수·시간을 재고 난이도별 최고 기록을 localStorage에 저장합니다.

`sentence-game/` 폴더에는 문장 빈칸 채우기 게임이 들어 있습니다 (`index.html` 열면 실행).
문장의 뼈대(`sk`)에 해당하는 단어는 그대로 보여주고, 뼈대에 없는 나머지 단어(수식어)는
전부 빈칸으로 바꿉니다. 문장부호·숫자는 빈칸으로 만들지 않고 그대로 둡니다. 화면 아래에
그 빈칸들에 들어갈 단어를 뒤섞어 늘어놓고, 빈칸을 먼저 고른 뒤 아래에서 맞는 단어를 골라
채우는 방식입니다. 다 채우면 문장 전체 번역(`ko`)이 공개되고 다음 문장으로 넘어갑니다.
`red-headed-league.html`의 `sentences`(`t`/`sk`/`skk`/`ko`)와 `passages`(어느 문장이 몇 번째
대목인지)를 읽어서 `sentence-game/sentdata.js`(문장마다 `t,sk,skk,ko,pi`— `pi`는 대목 번호)로
만들고, `skeletonHits()`와 완전히 같은 LCS 정렬 로직을 `sentence-game/game.js`에 그대로
복사해서 빈칸 위치를 계산합니다. 뼈대와 원문이 같아서 빈칸이 안 생기는 문장, 빈칸이 1개
이하인 문장은 문제 풀에서 제외합니다(전체 114문장 중 94문장이 대상, 문장당 빈칸 수는
2~39개로 차이가 큽니다 — 너무 어려운 문장은 "건너뛰기"로 넘어가면 됩니다).

**읽은 대목까지만 출제**: `red-headed-league.html`이 쓰는 localStorage 키 `rhl`의 `seen` 값을
그대로 읽어와서(쓰지는 않음), 아직 읽지 않은 대목(`pi > seen`)의 문장은 문제 풀에서 뺍니다.
읽은 대목이 하나도 없으면(`seen`이 없거나 0이면) "아직 읽은 대목이 없습니다" 안내만 뜹니다.

**난이도 3단계**: 게임 시작 전에 카드 매칭 게임처럼 난이도를 고르는 화면이 뜹니다.
기본(오답 없음) / 도전(오답 +30%) / 고난도(오답 +50%). 오답 단어는 `word-game/words.js`의
`WORDS` 배열(= `dict`)에서 이 문장의 정답과 겹치지 않는 단어를 무작위로 뽑아 빈칸 개수와
함께 섞어 보여줍니다. 문장마다 빈칸 수가 2~39개로 편차가 커서 퍼센트를 그대로 곱하면
안 되므로(짧은 문장은 체감이 안 되고, 긴 문장은 오답이 폭증함), **오답 개수 = round(빈칸수 ×
비율)을 최소 2개, 최대 8개로 자릅니다.** 예: 빈칸 2개 문장의 고난도(50%)는 1개가 아니라
최소값 2개, 빈칸 39개 문장의 고난도는 20개가 아니라 최대값 8개가 붙습니다. 고른 난이도와
진행 위치(`idx`)는 `rhl_sentgame`이라는 별도의 localStorage 키에 저장됩니다.

진행 위치: 홈즈가 다섯 가지 추리를 다 풀어놓자 윌슨이 웃음을 터뜨리고, 홈즈가 "Omne
ignotum pro magnifico"(모르는 것은 다 대단해 보이는 법)라며 자신을 낮춘 뒤, 윌슨이
그 유명한 "RED-HEADED LEAGUE" 신문 광고문(에제키아 홉킨스의 유증으로 생긴 결원,
자격 요건, 지원 방법)을 실제로 낭독합니다. 왓슨이 놀라서 외치고, 홈즈가 킬킬 웃으며
윌슨에게 처음부터 자초지종을 들려달라고 청하고, 광고 게재 날짜(모닝 크로니클, 1890년
4월 27일)를 확인한 뒤, 윌슨이 코버그 스퀘어에서 꾸리는 전당포 사업과 조수 빈센트
스폴딩 이야기(나이를 가늠하기 어려움, 반값 급여로 일함, 사진 취미에 빠져 지하실에서
현상 작업을 함)를 들려줍니다.
홈즈가 조수가 지금도 있는지 묻자, 윌슨은 아내 없이 홀로 지내며 스폴딩과 집안일을 돕는
열네 살 소녀 하나만 데리고 검소하게 지낸다고 답합니다. 그러다 8주 전 스폴딩이 신문을
들고 와 "제가 빨간 머리였으면"이라고 말한 게 이 모든 일의 발단이었다며, 윌슨이 그날
있었던 대화(스폴딩이 레드헤디드 맨스 리그의 결원 얘기를 꺼내고, 윌슨이 "그게 뭔데?"라고
되묻는 장면)를 그대로 재연합니다.
윌슨이 그 리그를 들어본 적 없다고 답하자 스폴딩이 놀라며, 에제키아 홉킨스라는 미국인
백만장자가 남긴 유산으로 신탁 관리인들이 빨간 머리 남자들에게 편한 일자리("berths")를
주는 단체라고 설명하고, 일도 거의 없이 연봉이 대략 "1년에 200파운드쯤"이라는 이야기로
이어집니다. 이어서 스폴딩은 자격 제한도 설명합니다 — 런던에 살고, 성인 남성이어야 하고,
머리색도 옅거나 칙칙한 빨강이 아니라 "진짜 밝고 타오르는 듯한, 불꽃 같은 빨강"이어야만
된다는 대목(10번째 대목, 91번 문장)까지 다룬 뒤,
윌슨이 결원이 하나 남았다는 말에 지원을 결심하고 스폴딩과 함께 포프스 코트로 찾아가는
장면으로 이어집니다. "북쪽, 남쪽, 동쪽, 서쪽에서" 빨간 머리를 가진 사람들이 죄다 몰려나와
플리트 가가 "빨간 머리 사람들로 발 디딜 틈이 없이"(choked with red-headed folk) 붐비고,
포프스 코트는 "노점상의 오렌지 손수레"(coster's orange barrow) 같았다는 묘사와 함께,
스폴딩이 인파를 밀고 당기고 부딪치며 윌슨을 계단까지 끌고 가 마침내 사무실에 들어서는
대목까지 나갔습니다(11번째 대목, 103번 문장까지).
원문에서 다음은 사무실 안 풍경으로 이어집니다 — 의자 두 개와 소나무 탁자뿐인 초라한 방,
그 뒤에 앉은 윌슨보다도 더 빨간 머리를 한 작은 남자(나중에 던컨 로스로 밝혀짐)가 지원자마다
트집을 잡아 떨어뜨리다가, 윌슨 차례가 되자 스폴딩이 소개하고, 이 남자가 "더할 나위 없이
적합하다"며 감탄하고 뒤로 물러섰다 다시 달려들어 손을 꽉 쥐고 축하해주는 대목까지
나갔습니다(12번째 대목, 113번 문장까지).
원문에서 다음은 이 남자가 정말로 윌슨의 머리카락을 잡아당겨 진짜인지 확인하는 장면
("It would be injustice to hesitate" 하며 양손으로 머리를 잡아 뜯듯 당기는 유명한 대목),
자신을 던컨 로스라고 소개하며 결혼 여부를 묻고(미혼이라 하자 잠시 실망했다가 그래도
괜찮다며), 오전 10시부터 오후 2시까지 브리태니커 백과사전을 필사하는 대가로 주급
4파운드를 주겠다고 제안하는 장면으로 이어집니다. 그 뒤로는 윌슨이 몇 주간 성실히 일하며
매주 토요일 금화 넉 냥을 받다가, 던컨 로스가 점점 뜸하게 들르더니 결국 발길을 끊는
대목(리그가 갑자기 사라지는 사건의 복선)까지 이어집니다.

원문 출처: Project Gutenberg #1661
`https://www.gutenberg.org/files/1661/1661-h/1661-h.htm`
(단편이라 장 구분은 없습니다. 전체 약 10,500단어, 지금까지 옮긴 104문장은 약 2,000단어 —
전체의 20% 정도 진행된 것으로 추산됩니다.)

브라이언님이 Gutenberg #1661(The Adventures of Sherlock Holmes) 전체를 저장한 사본이
`F:\EnglishGame\source\The Adventures of Sherlock Holmes _ Project Gutenberg.html`에
있습니다. WebFetch로 매번 인터넷에서 긴 원문을 인용하면 안전 필터에 걸릴 수 있으므로,
앞으로는 이 로컬 파일을 device_stage_files로 작업공간에 가져와서 그 안에서
"The Red-Headed League" 부분을 찾아 다음 대목 원문을 확인하는 방식을 씁니다.

## 파일 구조

HTML 안의 `<script id="data" type="application/json">` 블록에 모든 데이터가 있습니다.
화면 코드는 그 아래 `<script>`에 있고, 대개 손댈 일이 없습니다.

```
dict       : { "단어": ["품사", "뜻", "발음기호(IPA)"] }
passages   : [ { title, paras: [[문장번호, ...], ...] } ]   // paras 하나가 한 문단
sentences  : [ { t, sk, skk, ko, c, n } ]
quiz       : [ { p, q, o, a, e } ]
```

`dict`의 발음기호는 슬래시 없이 순수 IPA 문자열만 저장하고(예: `"kɔld"`), 화면에서
표시할 때 슬래시를 붙입니다(`/kɔld/`). CMU Pronouncing Dictionary(공개 발음 데이터)를
ARPAbet → IPA로 변환해서 만들었고, 강세는 1차 강세(ˈ)만 표시하고 2차 강세는 생략해서
단순하게 유지했습니다. 사전에 없는 단어(고유명사, 복합어 등)는 구성 요소를 나눠 찾거나
수동으로 채웠습니다. 새 대목에서 `dict`에 단어를 추가할 때도 발음기호를 같이 채워야 합니다
(CMU dict 기반 변환 스크립트를 다시 쓰거나, 국어사전/영어사전에서 IPA를 찾아 채우면 됩니다).

단어 탭 매칭은 화면 코드의 `cw()` 함수가 `[^A-Za-z'-]`를 다 지우고 비교합니다. 즉 é처럼
알파벳이 아닌 발음 부호가 붙은 글자는 통째로 사라집니다. 원문에 "employé"처럼 프랑스어투
철자가 나오면 dict 키는 그 결과물인 "employ"로 넣어야 탭했을 때 찾아집니다(실제 사전
키가 "employ"인 것도 이 때문입니다). 원문 자체는 고치지 않되, dict 키만 cw() 결과에 맞춥니다.

`sentences` 항목:

| 필드 | 내용 |
|---|---|
| `t` | 원문 그대로. 고치거나 현대어로 바꾸지 않는다 |
| `sk` | 수식어를 걷어낸 뼈대 (영어) |
| `skk` | 뼈대의 한국어 |
| `ko` | 문장 전체 번역 |
| `c` | 덩어리 배열. 각 항목은 `[원문조각, 역할, 한국어, attach]` |
| `n` | 이 문장이 왜 안 읽히는지. 쉬우면 빈 문자열 |

`attach`는 이 덩어리가 꾸미는 **대상 단어를 원문 그대로** 적습니다.
화면에서 덩어리를 탭하면 본문의 그 단어가 노랗게 표시됩니다.
문장 전체에 걸리면 `null`.
반드시 그 문장 안에 실제로 존재하는 단어여야 매칭됩니다.

`quiz` 항목: `p`는 출제 대상 대목 번호(0부터), `o`는 보기 배열,
`a`는 정답 인덱스, `e`는 해설.

### word-game/ (단어 매칭 게임, 별도 폴더)

```
index.html   화면 뼈대
style.css    스타일
game.js      게임 로직
words.js     var WORDS = [ {en, pos, ko, ipa}, ... ]  ← red-headed-league.html의 dict에서 생성
```

`words.js`는 `red-headed-league.html`의 `dict`를 그대로 옮긴 것입니다.
**새 대목을 추가해서 `dict`에 단어를 더할 때마다 `word-game/words.js`도 같이 갱신해야 합니다.**
(dict의 `["품사","뜻 · 부가설명"]` 형식 중 `·` 뒤에 주의사항이 붙은 항목은
카드에 쓸 짧은 뜻만 골라 옮겨야 합니다 — 예: `"positive":["형","확실한, 명백한 · ..."]` → 카드에는 "확실한, 명백한"만.
`"call upon · ~를 찾아가다"`처럼 첫 조각이 영어 원형/문법 설명이면 그 다음 한글 조각을 씁니다.)

## 다음에 할 일

1. **다음 대목 추가** — 원문에서 이어지는 10~15문장.
   `sentences`에 항목을 추가하고, `passages`에 새 대목을 넣고,
   새 단어를 `dict`에, 문제 2~3개를 `quiz`에 추가.
2. 새 단어를 `dict`에 추가했으면(발음기호 포함) **`word-game/words.js`도 같이 갱신**.
3. 대목을 늘릴 때마다 브라우저로 열어 확인.
4. 어느 정도 쌓이면 실제로 읽어보고 화면을 손본다.

## 작업 규칙

- **한 번에 10~15문장씩.** 통째로 만들려 하면 데이터 작업만 며칠 걸리고
  그동안 정작 읽지를 못한다.
- **원문을 지어내지 않는다.** 위 Gutenberg 주소에서 실제 문장을 가져온다.
- `role`은 문법 용어를 피하고 자리와 기능으로 쓴다.
  "형용사절"이 아니라 "앞 명사를 꾸밈".
- `n`에는 단어 뜻을 넣지 않는다. 구조 이야기만.
- 문장이 쉬우면 억지로 분해하지 않는다. 짧은 대화문은 덩어리 2~3개면 충분하다.
- **필드를 추가하거나 이름을 바꾸지 않는다.** 바꾸면 기존 문장을 전부 다시 만들어야 한다.
  정말 필요하면 그때 전체를 한 번에 옮긴다.

## 나중에 할 것 (지금은 하지 않음)

- 복습 간격 계산: 틀린 문장은 이틀 뒤, 맞힌 문장은 일주일 뒤에 다시 출제
- 찾아본 단어 목록 화면
- 클라우드에 올려 스마트폰에서 접근
- 어순 배열, 단어 끼워넣기 같은 문장 게임 — `sentences` 데이터를 그대로 쓸 수 있음
- word-game: 읽은 대목의 단어만 출제하는 필터, 오답 단어 다시 나오게 하기
